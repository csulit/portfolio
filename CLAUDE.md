# Claude Code Guidelines

## Development Server & Build

- **Do NOT** run `bun run dev`, `bun run start`, or any development server commands without explicit user approval.
- **Do NOT** run `bun run build` or any build commands without explicit user approval.
- Always ask before executing these commands.
